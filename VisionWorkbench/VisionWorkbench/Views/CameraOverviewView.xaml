<UserControl x:Class="VisionWorkbench.Views.CameraOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:imagebox="clr-namespace:ImageBox;assembly=ImageBox"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="980">
    <Grid>
        <ItemsControl ItemsSource="{Binding Cameras}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" Rows="2" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Margin" Value="4" />
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource PanelBorder}" ClipToBounds="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Background="{StaticResource SurfaceMutedBrush}"
                                    BorderBrush="{StaticResource AppBorderBrush}"
                                    BorderThickness="0,0,0,1"
                                    Padding="10,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Ellipse Width="8"
                                             Height="8"
                                             Fill="{Binding ResultBrush}"
                                             VerticalAlignment="Center"
                                             Margin="0,0,8,0" />

                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextBrush}" />
                                    </StackPanel>

                                    <Border Grid.Column="2"
                                            Background="{Binding ResultBackBrush}"
                                            CornerRadius="4"
                                            Padding="8,2"
                                            VerticalAlignment="Center"
                                            Margin="6,0"
                                            Visibility="{Binding HasResult, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="{Binding Result}"
                                                   Foreground="{Binding ResultBrush}"
                                                   FontWeight="SemiBold"
                                                   FontSize="12" />
                                    </Border>

                                    <Button Grid.Column="3"
                                            Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.OpenCameraCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="单相机">
                                        <TextBlock Style="{StaticResource IconGlyph}" Text="&#xE8A7;" />
                                    </Button>
                                </Grid>
                            </Border>

                            <imagebox:ImageBoxControl Grid.Row="1"
                                                      ImageSource="{Binding Frame}"
                                                      OverlayItems="{Binding Overlays}"
                                                      InteractionMode="Pan"
                                                      ShowInfoOverlay="False"
                                                      ShowBrightnessInfo="False" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
